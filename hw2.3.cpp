#include <stdio.h>
/*
void construct(double array[14]) {
    printf("Enter 14 numbers:\n");
    for (size_t i = 0; i < 14; ++i) {
        double numb;
        scanf("%lf", &numb);
        array[i] = numb;
    }
}

void display(double array[14]) {
    printf("\nEntered array:\n");
    for (size_t i = 0; i < 14; ++i) {
        printf("%lf ", array[i]);
    }
    printf("\n\n");
}

void minim_extra(double array[14]) {
    double minim = array[0] - array[13];
    for (size_t i = 0; i < 7; ++i) {
        printf("%lf - %lf = %lf\n", array[i], array[14 - i - 1], array[i] - array[14 - i - 1]);
        if (array[i] - array[14 - i - 1] < minim) {
            minim = array[i] - array[14 - i - 1];
        }
    }
    printf("\nMin element: %lf", minim);
}

int main() {
    double array[14];
    construct(array);
    display(array);
    minim_extra(array);
    return 0;
}
*/