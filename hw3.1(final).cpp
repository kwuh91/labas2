#include <stdio.h>
#include "hw3.1(final).h"
#include "hw3.1_test(final).h"
/*
int main(){
    printf ("Enter:\n 1 - choose a function\n 2 - exit program\n");
    int user_input;
    while (true){
        scanf("%d",&user_input);
        switch (user_input){
            case 1:
                display_funcs();
                int user_input2;
                int n;
                double a;
                double b;
                printf ("Choose one:\n");
                scanf("%d",&user_input2);
                printf("Enter number of splits:\n");
                scanf("%d",&n);
                printf ("Enter interval [a,b] a:\n");
                scanf("%lf",&a);
                printf ("Enter interval [a,b] b:\n");
                scanf("%lf",&b);
                TRAP(n,a,b,user_input2);
                printf ("\n");
                printf ("Enter:\n 1 - choose a function\n 2 - exit programm\n");
                break;
            case 2:
                exit(0);
            default:
                printf("Invalid option\n");
                break;
        }
    }
}
*/